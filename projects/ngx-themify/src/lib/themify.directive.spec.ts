import { ThemifyDirective } from './themify.directive';

describe('ThemifyDirective', () => {
  it('should create an instance', () => {
    const directive = new ThemifyDirective();
    expect(directive).toBeTruthy();
  });
});
